Engineered a web-based routing tool modeling a global airport network as a weighted directed graph, implementing Dijkstra's and A* in Java to compute optimal routes by distance, time, or cost
Modified shortest-path algorithms to track hop count alongside cost, constraining routes to a user-specified number of layovers
Built an interactive Leaflet front end (HTML/CSS/JS) linking to the Java backend, visualizing routes and benchmarking nodes explored/runtime across both algorithms

javac *.java
java StudentCode
